// importa tu clase, instancia al menos tres objetos e imprime los 
//atributos name de todos.

const MissionCommander = require('./app/missionCommander');

// Objeto 1
const carlo = new MissionCommander("Carlo")
console.log(carlo.name)

// Objeto 2
const herrCode = new MissionCommander("Herr-code")
console.log(herrCode.name)

// Objeto 3
const teresa = new MissionCommander("Teresa")
console.log(teresa.name)